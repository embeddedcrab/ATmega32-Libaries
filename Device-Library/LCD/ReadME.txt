
This library is for alphanumeric 16X2 LCD. class _LCD contains all the prototypes which might be the possible functions which can
be done using it. Comments are written for each function i.e, how the process is going.

You just have to declare an object of _LCD in main and then simply call member functions according to need. This library can easily
be converted into C code, justneed to use your mind.
